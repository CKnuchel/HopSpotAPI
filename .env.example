# Server
PORT=8080
LOG_LEVEL=INFO

# Database
DB_USER=bench_user
DB_PASSWORD=CHANGE_ME
DB_NAME=bench_db

# Authentication
JWT_SECRET=CHANGE_ME
JWT_EXPIRE_SECONDS=900
JWT_ISSUER=hopspot
JWT_AUDIENCE=hopspot_users
REFRESH_TOKEN_EXPIRE_DAYS=90

# MinIO
MINIO_ROOT_USER=minio_admin
MINIO_ROOT_PASSWORD=CHANGE_ME
MINIO_BUCKET_NAME=hopspot-photos

# Firebase
FIREBASE_AUTH_KEY=CHANGE_ME_BASE64_ENCODED

# Redis (optional - API works without it, just no caching)
REDIS_HOST=redis                            # Use 'localhost' for local dev without Docker
REDIS_PORT=6379
REDIS_PASSWORD=                             # Leave empty if no auth
REDIS_DB=0

# Cache Settings
WEATHER_CACHE_TTL_MINUTES=15                # Weather data cache duration